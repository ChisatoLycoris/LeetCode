package patterns;

/**
 * Marker interface for problems using depth-first search (DFS).
 * DFS explores as far as possible along each branch before backtracking.
 */
public interface DepthFirstSearchPattern {
}
