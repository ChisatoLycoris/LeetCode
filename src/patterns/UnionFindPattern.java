package patterns;

/**
 * Marker interface for problems using the Union-Find (Disjoint Set Union) data structure.
 * Union-Find efficiently tracks a partition of elements into disjoint sets,
 * supporting near-constant-time union and connectivity queries via path compression and union by rank.
 */
public interface UnionFindPattern {
}
