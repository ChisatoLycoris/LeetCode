package patterns;

/**
 * Marker interface for problems using sliding window technique.
 * Sliding window maintains a subset of elements and slides through the array/string to find optimal solutions.
 */
public interface SlidingWindowPattern {
}
