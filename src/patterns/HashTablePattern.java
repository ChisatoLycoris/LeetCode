package patterns;

/**
 * Marker interface for problems using hash tables/maps.
 * Hash tables provide O(1) average-case lookup, insertion, and deletion through key-value mappings.
 */
public interface HashTablePattern {
}
