package patterns;

/**
 * Marker interface for problems using binary search algorithm.
 * Binary search efficiently finds targets in sorted data by repeatedly dividing the search space in half.
 */
public interface BinarySearchPattern {
}
